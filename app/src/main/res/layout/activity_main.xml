<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="16dp"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:background="#2E2E2E"
    tools:context="com.testapp.geoffreycaven.weatherapp.MainActivity">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="10dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:id="@+id/location_preview"
        android:text="No Location Selected"
        android:textAppearance="@android:style/TextAppearance.Material.Button"
        android:textAlignment="center"
        android:gravity="center_vertical"
        android:textColor="#FFFFFF"/>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:layout_below="@+id/change_location_button">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/entry_view">

        </RelativeLayout>

    </ScrollView>

    <Button
        android:layout_width="match_parent"
        android:id="@+id/change_location_button"
        android:text="Change Location"
        android:layout_height="50dp"
        android:layout_marginBottom="10dp"
        android:onClick="onClickChangeLocation"
        android:layout_below="@+id/location_preview"
        android:layout_alignParentStart="true" />
</RelativeLayout>
